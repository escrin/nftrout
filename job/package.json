{
  "scripts": {
    "lint:prettier": "prettier --check --plugin-search-dir=. --cache '*.json' 'src/**/*.ts'",
    "lint": "npm-run-all lint:**",
    "format:prettier": "prettier --write --plugin-search-dir=. --cache '*.json' 'src/**/*.ts'",
    "format": "npm-run-all format:**",
    "build:typecheck": "tsc -b",
    "build:bundle": "esbuild src/nftrout.ts --bundle --outfile=bin/nftrout --platform=node --target=node18 --minify --sourcemap=inline",
    "build": "npm-run-all --parallel build:**"
  },
  "dependencies": {
    "@escrin/evm": "1.0.0-beta.2",
    "@oasisprotocol/deoxysii": "^0.0.5",
    "@oasisprotocol/sapphire-paratime": "^1.0.15",
    "canonicalize": "^2.0.0",
    "ethers": "^5.7.2",
    "keccak": "^3.0.3",
    "nft.storage": "^7.0.3"
  },
  "devDependencies": {
    "@types/keccak": "^3.0.1",
    "@types/node": "^18.15.11",
    "esbuild": "^0.17.14",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.0.0"
  }
}
